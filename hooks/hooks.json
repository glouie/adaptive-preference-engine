{
  "description": "Adaptive Preference Engine hooks — session start registry and preference signal detection",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PLUGIN_ROOT/scripts/session-start-hook.sh"
          }
        ]
      },
      {
        "_comment": "WORKAROUND: Was type:prompt but prompt hooks are broken (anthropics/claude-code#39184). Using command-type + shell echo as workaround. TODO: Revert to type:prompt when #39184 is fixed. Original prompt preserved in prompt-workaround.sh.",
        "hooks": [
          {
            "type": "command",
            "command": "bash $CLAUDE_PLUGIN_ROOT/hooks/prompt-workaround.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 $CLAUDE_PLUGIN_ROOT/scripts/posttool-signal-detector.py"
          }
        ]
      }
    ]
  }
}
